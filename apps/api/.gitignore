# Cliente generado: se regenera con `prisma generate`, no se versiona.
src/platform/database/generated/
node_modules/
.env
dist/
coverage/
*.tmp.ts
*.log.tmp

# Documentos generados por el worker. Son artefactos: se regeneran desde los
# datos, pesan y además pueden contener información del plan que no tiene por
# qué viajar en el repositorio.
var/
