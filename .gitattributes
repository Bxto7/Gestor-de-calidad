# Normaliza finales de línea: LF en el repositorio, nativo en el working copy.
# Sin esto, un equipo mixto Windows/Linux genera diffs de archivos completos.
* text=auto eol=lf

# Binarios: nunca tocar.
*.png binary
*.jpg binary
*.woff2 binary
*.pdf binary
